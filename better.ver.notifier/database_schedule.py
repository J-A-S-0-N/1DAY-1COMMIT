#import mysql

#connect the sql
#create sql curser

def print_schedule():
    # get the row
    
    # for:
    
    #   print(row) 

def new_schedule(new_name, new_time, new_message):
    # curser.save the new_name
    
    # curser.save the new_time

    # curser.save the new message

def open_schedule(open_name):
    # curset.find the row of the which the name is name
    
    # save the name time message as an variable to the main.py file

def delete(delete_name):
    # curser.find the row of the which the name is name
    
    # delete the row which contain the name

def get_all_item():
    # curser.get all time collem